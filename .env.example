# Database path (relative to project root)
DATABASE_PATH=priv/repo/live_board_dev.db

# Server port
PORT=4000

# Secret key base (generate with: mix phx.gen.secret)
# A dev default is built-in; set this for production
SECRET_KEY_BASE=
